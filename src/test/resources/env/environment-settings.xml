<settings>

    <servers>
        <server>
            <id>environment-server</id>
            <username>${env.SERVER_USERNAME}</username>
            <password>${env.SERVER_PASSWORD}</password>
        </server>
    </servers>

</settings>
